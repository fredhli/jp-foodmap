// Auto-generated by src/tabelog/scrape/map.py — do not edit by hand.
// Build version: 1788759480
const VERSION = '1788759480';
// Shell cache is per-deploy (holds the HTML); the other two persist across
// deploys — their entries are content-addressed (?v= hash) or immutable.
const SHELL_CACHE = 'tabelog-shell-' + VERSION;
const DATA_CACHE  = 'tabelog-data-v1';
// M-006: bumped to v2 alongside the M-058 tile-regex fix. A cache NAME is not
// a localStorage key — no user data lives here, and activate() drops v1.
const EXT_CACHE   = 'tabelog-ext-v2';
// M-144 + M-011: basemap tiles get their OWN cache. They were sharing
// EXT_CACHE with MarkerCluster's JS/CSS, and once M-058 made the tile
// pattern actually match, a minute of panning pushed 70+ tiles through the
// LRU and evicted the very CDN assets M-011 put there so an offline boot
// could still build the cluster layer. Measured: 72/72 entries were tiles.
// M-162: bumped to v2 alongside the crossOrigin switch on the TileLayer. Every
// entry a v1 worker wrote is an OPAQUE response that Chrome bills at ~4.2MB of
// padded quota (measured; 127x the real 33KB). Those entries are unusable to a
// CORS tile request and would otherwise sit in the bucket forever, so the name
// changes and activate()'s existing KEEP sweep reclaims ~300MB in one go.
const TILE_CACHE  = 'tabelog-tiles-v2';
const KEEP = [SHELL_CACHE, DATA_CACHE, EXT_CACHE, TILE_CACHE];

// Versioned with SHELL_CACHE so launcher metadata and icons update with a
// deploy. Caching the root document during install makes the very first
// installed-app launch work even if the phone has already gone offline.
// M-061: the launcher icons (142 KB) are only ever painted by an installed
// app, and the overwhelming majority of visitors never install — so they
// moved out of the unconditional set and are warmed only when this really
// is (or has just become) an installed launch.
const APP_SHELL_URLS = [
  './',
  './manifest.webmanifest?v=shortcuts-2',
];
const LAUNCHER_ICON_URLS = [
  './icons/icon-japan-emoji-v2-192.png',
  './icons/icon-japan-emoji-v2-512.png',
  './icons/icon-japan-emoji-v2-maskable-512.png',
  './apple-touch-icon-japan-emoji-v2.png',
];

// What install warms (subset: the boot-critical payloads for the default
// language) and everything the current build references with a ?v= hash
// (used by activate's garbage collection — entries outside this list are
// superseded versions and get dropped so 6MB payloads don't pile up).
const PRECACHE_URLS = ["data/restaurants.json?v=6b88bd4268", "transit-layer.js?v=843a5dcc02", "vendor/leaflet-1.9.3/leaflet.js?v=f8b1586d92", "vendor/leaflet-1.9.3/leaflet.css?v=b38a4dc72f", "vendor/leaflet.markercluster-1.1.0/leaflet.markercluster.js?v=ddbf117703", "vendor/leaflet.markercluster-1.1.0/MarkerCluster.css?v=e93c23ed32", "vendor/leaflet.markercluster-1.1.0/MarkerCluster.Default.css?v=3b9131edb9", "vendor/leaflet.locatecontrol-0.79.0/L.Control.Locate.min.js?v=62bb1d8d8c", "vendor/leaflet.locatecontrol-0.79.0/L.Control.Locate.min.css?v=e6109be62f"];
const CURRENT_VERSIONED_URLS = ["data/restaurants.json?v=6b88bd4268", "data/popups.json?v=07b21de6cf", "data/popups-tw.json?v=03b59f49cb", "data/popups-en.json?v=ba23622e8f", "data/popups-ja.json?v=88fefca6f0", "transit-layer.js?v=843a5dcc02", "vendor/leaflet-1.9.3/leaflet.js?v=f8b1586d92", "vendor/leaflet-1.9.3/leaflet.css?v=b38a4dc72f", "vendor/leaflet.markercluster-1.1.0/leaflet.markercluster.js?v=ddbf117703", "vendor/leaflet.markercluster-1.1.0/MarkerCluster.css?v=e93c23ed32", "vendor/leaflet.markercluster-1.1.0/MarkerCluster.Default.css?v=3b9131edb9", "vendor/leaflet.locatecontrol-0.79.0/L.Control.Locate.min.js?v=62bb1d8d8c", "vendor/leaflet.locatecontrol-0.79.0/L.Control.Locate.min.css?v=e6109be62f", "vendor/emoji-picker-element-1.27.0/index.js?v=28d9bac25c"];
// M-012: the minimum set this build needs in order to boot with no network.
// Vendored boot-critical assets are appended to this list at build time.
// Anything in here (plus APP_SHELL_URLS) is all-or-nothing during install.
const CRITICAL_URLS = ["./", "vendor/leaflet-1.9.3/leaflet.js?v=f8b1586d92", "vendor/leaflet-1.9.3/leaflet.css?v=b38a4dc72f", "vendor/leaflet.markercluster-1.1.0/leaflet.markercluster.js?v=ddbf117703", "vendor/leaflet.markercluster-1.1.0/MarkerCluster.css?v=e93c23ed32", "vendor/leaflet.markercluster-1.1.0/MarkerCluster.Default.css?v=3b9131edb9"];

self.addEventListener('install', (event) => {
  event.waitUntil((async () => {
    // M-012: this used to swallow every per-URL failure and then call
    // skipWaiting() unconditionally, so a deploy fetched during a captive
    // portal / 5xx window produced an EMPTY shell cache that immediately
    // activated and had activate() delete the last working one — offline
    // cold start went white. Now the boot-critical set is atomic: any
    // rejection here rejects install, the new worker never activates, and
    // the previous worker + its caches stay exactly as they were.
    const bootCritical = [];
    APP_SHELL_URLS.concat(CRITICAL_URLS).forEach((u) => {
      if (bootCritical.indexOf(u) === -1) bootCritical.push(u);
    });
    // Fetch everything before opening the cache, so a rejected install
    // doesn't even leave an empty tabelog-shell-<version> behind.
    const fetched = await Promise.all(bootCritical.map(async (u) => {
      // M-061: no cache:'reload' for './' — the navigation that just
      // registered this worker already wrote the very same HTML into
      // this very same cache via networkFirst, and 'reload' explicitly
      // bypasses the HTTP cache, so it was a guaranteed second full
      // download of the document on every deploy.
      const resp = await fetch(u);
      if (!resp || !resp.ok) {
        throw new Error('sw install: ' + u + ' -> ' +
                        (resp ? resp.status : 'no response'));
      }
      return [u, resp];
    }));
    const shell = await caches.open(SHELL_CACHE);
    await Promise.all(fetched.map(([u, resp]) => shell.put(u, resp)));
    try {
      const cache = await caches.open(DATA_CACHE);
      // Content-addressed: if the hash matches an entry we already hold,
      // the file didn't change — skip the network entirely. Fail-soft per
      // URL (these are big optional payloads, not the boot path); a miss
      // just falls back to cacheFirst on first fetch.
      await Promise.all(PRECACHE_URLS.map(async (u) => {
        try {
          if (await cache.match(u)) return;
          const resp = await fetch(u);
          if (resp && (resp.ok || resp.type === 'opaque')) await cache.put(u, resp);
        } catch (_) {}
      }));
    } catch (_) { /* opening the cache failed — install still proceeds */ }
    // M-012: no skipWaiting() here. The new worker sits in `waiting` until
    // the page shows "a new version is available" and the user accepts;
    // only then does it get a SKIP_WAITING message (see the message
    // handler). An unattended swap can drop an in-flight edit.
  })());
});

self.addEventListener('activate', (event) => {
  event.waitUntil((async () => {
    // M-012: never delete the previous shell on the strength of a shell
    // cache that does not actually hold the document. If './' is missing,
    // keep every other shell cache around — a stale page beats a blank one.
    let shellOk = false;
    try {
      const shell = await caches.open(SHELL_CACHE);
      shellOk = !!(await shell.match('./'));
    } catch (_) {}
    const keys = await caches.keys();
    const doomed = keys.filter(k => KEEP.indexOf(k) === -1);
    if (shellOk) {
      await Promise.all(doomed.map(k => caches.delete(k)));
    } else {
      console.warn('[sw] shell cache holds no "./" — keeping older shells');
      await Promise.all(doomed
        .filter(k => k.indexOf('tabelog-shell-') !== 0)
        .map(k => caches.delete(k)));
    }
    // GC superseded ?v= entries. Plain-URL entries (emoji PNGs,
    // requests from a not-yet-refreshed old page) are left alone.
    try {
      const wanted = new Set(CURRENT_VERSIONED_URLS.map(
        u => new URL(u, self.location.href).href));
      const cache = await caches.open(DATA_CACHE);
      for (const req of await cache.keys()) {
        const url = new URL(req.url);
        if (!/^\?v=/.test(url.search)) continue;
        if (!wanted.has(url.href)) await cache.delete(req);
      }
    } catch (_) {}
    await self.clients.claim();
  })());
});

// Page-driven warmups. The service worker can't know two things the page
// knows: which UI language is active (M-062 — popups is one 6.4MB file per
// language and install-time precaching guessed zh-CN for everyone), and
// whether this is an installed launch (M-061 — launcher icons). Both arrive
// as messages. Only URLs this build actually references are honoured, so a
// stray postMessage can't turn the cache into an open proxy.
function versionedHrefs() {
  const out = new Set();
  for (const u of CURRENT_VERSIONED_URLS) {
    try { out.add(new URL(u, self.location.href).href); } catch (_) {}
  }
  return out;
}
async function warmInto(cacheName, hrefs) {
  const cache = await caches.open(cacheName);
  await Promise.all(hrefs.map(async (h) => {
    try {
      if (await cache.match(h)) return;
      const resp = await fetch(h);
      if (resp && (resp.ok || resp.type === 'opaque')) await cache.put(h, resp);
    } catch (_) {}
  }));
}
self.addEventListener('message', (event) => {
  const d = event.data;
  if (!d || typeof d !== 'object') return;
  // M-012: the ONLY way a waiting worker takes over. Sent by the page after
  // the user taps "reload" on the update prompt.
  if (d.type === 'SKIP_WAITING') { self.skipWaiting(); return; }
  if (d.type === 'WARM_DATA' && typeof d.url === 'string') {
    event.waitUntil((async () => {
      try {
        const href = new URL(d.url, self.location.href).href;
        if (!versionedHrefs().has(href)) return;   // not part of this build
        await warmInto(DATA_CACHE, [href]);
      } catch (_) {}
    })());
    return;
  }
  if (d.type === 'WARM_LAUNCHER_ICONS') {
    event.waitUntil(warmInto(SHELL_CACHE, LAUNCHER_ICON_URLS).catch(() => {}));
  }
});

self.addEventListener('fetch', (event) => {
  const req = event.request;
  if (req.method !== 'GET') return;
  const url = new URL(req.url);

  if (url.origin === self.location.origin) {
    // sw.js itself must always come from the network so updates land.
    if (url.pathname.endsWith('/sw.js')) return;
    if (req.mode === 'navigate' || url.pathname.endsWith('.html')) {
      event.respondWith(networkFirst(req, event));
      return;
    }
    if (url.pathname.endsWith('/manifest.webmanifest') ||
        url.pathname.indexOf('/apple-touch-icon') !== -1 ||
        url.pathname.indexOf('/icons/') !== -1) {
      event.respondWith(cacheFirst(req, SHELL_CACHE));
      return;
    }
    // M-058: the /transit/ branch used to send docs/transit/japan.geojson
    // through stale-while-revalidate. That file moved to R2
    // (assets.jpfoodmap.com) long ago, so the branch has been dead code —
    // removed rather than left as a trap for the next reader.
    event.respondWith(cacheFirst(req));
    return;
  }

  // CDN assets we serve from cache on revisit but refresh in the
  // background, with an LRU cap so opaque entries (which Chrome pads to
  // ~7MB each for quota accounting) can't silently exhaust origin quota.
  // Anything outside this list (Nominatim search, jpfoodmap sync API)
  // passes straight through to the browser default.
  // M-011: cdnjs serves MarkerCluster's JS + both its stylesheets, and a
  // host that isn't listed here never reaches respondWith at all — so an
  // offline/blocked cdnjs left the boot hard-stuck on L.markerClusterGroup
  // with nothing in Cache Storage to fall back to.
  // M-058: the old tile pattern (`^[a-c]\.tile\.` / `.tile.openstreetmap.org`)
  // never matched a single request — the basemap has been
  // {s}.basemaps.cartocdn.com for as long as this worker has existed, so
  // offline panning showed grey squares even where tiles had been fetched.
  // M-135: unpkg.com dropped — nothing in this build requests it.
  // M-162: tiles are cache-first, not stale-while-revalidate. A tile is an
  // immutable bitmap behind `cache-control: public,max-age=15552000` (180
  // days) — there is nothing to revalidate, and SWR's unconditional second
  // fetch doubled the request count during a pan (measured 104 vs 72).
  if (/(\.tile\.openstreetmap\.org$)|(\.basemaps\.cartocdn\.com$)/i.test(url.hostname)) {
    event.respondWith(tileCacheFirst(req));
    return;
  }
  if (url.hostname === 'emojicdn.elk.sh' ||
      url.hostname === 'cdn.jsdelivr.net' ||
      url.hostname === 'cdnjs.cloudflare.com') {
    event.respondWith(staleWhileRevalidate(req, EXT_CACHE, EXT_CACHE_MAX));
  }
});

// Opaque (cross-origin no-cors) responses report ok=false / status=0 but
// we still want them cached for revisits.
function cacheable(resp) {
  return !!resp && (resp.ok || resp.type === 'opaque');
}

// M-049: a static host answers an unknown path with the 200 HTML of an error
// page. Cached under data/foo.json?v=… that becomes a permanent poison entry
// — JSON.parse throws forever, and the ?v= hash means it is never refetched.
// Extension says asset, content-type says document ⇒ refuse to store it.
const ASSET_PATH_RE = /\.(json|png|jpg|jpeg|webp|js|css)$/i;
function cacheableAsset(req, resp) {
  if (!resp) return false;
  if (resp.type === 'opaque') return true;   // cross-origin: nothing to read
  if (!resp.ok) return false;
  try {
    if (ASSET_PATH_RE.test(new URL(req.url).pathname)) {
      const ct = resp.headers.get('content-type') || '';
      if (/^\s*text\/html/i.test(ct)) return false;
    }
  } catch (_) {}
  return true;
}

// M-144 said 70 opaque tiles were "an order of magnitude under any device
// quota". Measured, that was wrong by two orders: Chrome pads an opaque
// cross-origin entry to 4.24MB (padding.py: 10 tiles = 42,366,131 bytes vs
// 324,128 bytes of real payload — 127x), so 70 entries ≈ 297MB of NOMINAL
// quota. On a phone with ~200MB of origin quota the first screenful already
// bills 100.7%, every later cache.put fails silently, and the origin — which
// includes localStorage, i.e. omakase_state_cache_v2 / tabelog.bookmarks /
// tabelog.syncBase — becomes the fattest eviction candidate on the device.
// M-162 fixes the billing instead of the cap: the TileLayer now sends
// crossOrigin="anonymous", the responses are real CORS responses billed at
// their true ~33KB, and 400 entries ≈ 13MB — enough for two or three
// screenfuls at keepBuffer:4 while staying genuinely negligible.
// The CDN cache keeps its own, smaller-but-separate cap: it holds the
// boot-critical MarkerCluster bundle, which must not be evicted by a pan.
const TILE_CACHE_MAX = 400;
const EXT_CACHE_MAX  = 120;
// ...and the trim itself can't be a 2% coin flip any more: at 70 entries a
// run of unlucky rolls overshoots the cap by hundreds. Deterministic counter.
const TRIM_EVERY = 10;
// M-162: tiles and CDN assets count separately. Sharing one counter meant a
// pan's tile puts kept firing trimCache() against the CDN bucket (a full
// cache.keys() enumeration each time) and vice versa.
let extPutCount  = 0;
let tilePutCount = 0;
async function trimCache(cacheName, max) {
  try {
    const cache = await caches.open(cacheName);
    const keys = await cache.keys();   // insertion order — oldest first
    for (let i = 0; i < keys.length - max; i++) await cache.delete(keys[i]);
  } catch (_) {}
}

async function cacheFirst(req, cacheName = DATA_CACHE) {
  const cache = await caches.open(cacheName);
  const cached = await cache.match(req);
  if (cached) return cached;
  const fresh = await fetch(req);
  if (cacheableAsset(req, fresh)) cache.put(req, fresh.clone());   // M-049
  return fresh;
}

// M-162: basemap tiles. Cache-first with an LRU cap of its own — a hit never
// goes to the network (SWR used to refetch every single tile behind the
// screen it had just served) and a miss is stored on the way through.
// ONE rule holds the whole thing together: an OPAQUE tile is never stored.
// The cap of 400 is only affordable because a CORS tile is billed at its real
// ~33KB; an opaque one is padded to ~4.2MB, so 400 of them would be ~1.7GB of
// nominal quota — 5.7x worse than the 70-entry bucket this change exists to
// fix, and quota pressure evicts the whole origin, localStorage included
// (omakase_state_cache_v2 / tabelog.bookmarks / tabelog.syncBase).
// It also closes the poisoning hole cache-first opens: cacheableAsset()
// cannot read the status of an opaque response, so a 403 from a rate-limited
// CDN edge would be stored as a normal tile and, with no revalidate, served
// back for the rest of the bucket's life. Not storing it means the next pan
// simply refetches — the same self-healing SWR used to give us.
// Two paths still send no-cors tiles here and both are fine reading opaque
// entries but write nothing: a pre-M-162 tab that clients.claim() adopted
// during a deploy, and a load where TILE_CORS_FALLBACK_JS has tripped. They
// lose tile caching for that page load only (offline panning included) and
// recover on the next reload; the two hazards above are permanent.
// Reading is asymmetric on purpose: respondWith() turns an opaque response
// into a network error whenever the request mode isn't 'no-cors', so an
// opaque entry (from an older worker, or a bucket that predates this rule)
// must never answer a CORS request — it falls through to fetch instead.
async function tileCacheFirst(req) {
  const cache = await caches.open(TILE_CACHE);
  const cached = await cache.match(req);
  if (cached && (cached.type !== 'opaque' || req.mode === 'no-cors')) return cached;
  const fresh = await fetch(req);
  if (fresh.type !== 'opaque' && cacheableAsset(req, fresh)) {
    // Quota is finite even at 33KB/tile; a rejected put must not surface as
    // an unhandled rejection inside the fetch handler.
    cache.put(req, fresh.clone()).catch(function() {});
    if (++tilePutCount % TRIM_EVERY === 0) trimCache(TILE_CACHE, TILE_CACHE_MAX);
  }
  return fresh;
}

// Navigations prefer fresh HTML, but a returning user with a good cached
// copy shouldn't stare at a blank page while a flaky connection crawls to
// the browser's own timeout (roaming travelers are this site's core
// audience). If the network hasn't answered within NAV_TIMEOUT_MS and a
// cached copy exists, serve it; the fetch keeps running via waitUntil and
// still refreshes the cache for next time.
const NAV_TIMEOUT_MS = 3500;
async function networkFirst(req, event) {
  const cache = await caches.open(SHELL_CACHE);
  // M-149: every navigation renders the same document, so keying the cache
  // entry by the full URL stored one full copy of index.html per query-string
  // permutation (/?lang=en, /?r=<slug>, …). Navigations all collapse onto
  // './'; the read below still tries the exact request first, so entries
  // written by an older worker keep resolving.
  const key = (req.mode === 'navigate') ? './' : req;
  const fetchP = fetch(req).then((fresh) => {
    if (cacheable(fresh)) cache.put(key, fresh.clone());
    return fresh;
  });
  if (event) event.waitUntil(fetchP.catch(() => {}));
  // Root is pre-cached during installation. It is also the safe fallback
  // for an offline /index.html navigation or an unexpected same-scope URL.
  const cached = await cache.match(req) || await cache.match('./');
  if (!cached) return fetchP;
  let timer;
  try {
    return await Promise.race([
      // M-151: a 5xx resolves like any other response, so the old code
      // handed Cloudflare's error page to the navigation while a perfectly
      // good cached document sat one line away. Only an ok response wins.
      fetchP.then((fresh) => (fresh && fresh.ok) ? fresh : cached)
            .catch(() => cached),
      new Promise((res) => { timer = setTimeout(() => res(cached), NAV_TIMEOUT_MS); }),
    ]);
  } finally {
    clearTimeout(timer);
  }
}

async function staleWhileRevalidate(req, cacheName, maxEntries) {
  const cache = await caches.open(cacheName);
  const cached = await cache.match(req);
  const fresh = fetch(req).then(resp => {
    if (cacheableAsset(req, resp)) {   // M-049
      cache.put(req, resp.clone());
      // M-144: amortized trim — keys() on every put would be pure overhead,
      // but a deterministic every-Nth beats the old Math.random() < 0.02.
      if (maxEntries && (++extPutCount % TRIM_EVERY === 0)) {
        trimCache(cacheName, maxEntries);
      }
    }
    // M-151: never let a 5xx displace a good cached copy.
    return (resp && (resp.ok || resp.type === 'opaque')) ? resp : (cached || resp);
  }).catch(() => cached);
  return cached || fresh;
}
